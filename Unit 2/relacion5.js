var nImg;
var nLinks;

function contImg() { nImg = document.images.length; return nImg}
contImg();
console.log('Numero de imagenes: ' + nImg);

function contLinks { nLinks = document.links.length; return nLinks}
contLinks();
console.log("Numero de links: " + nLinks);

